# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:
# ⚡ Flash Course AI

[![Version](https://img.shields.io/badge/version-1.1.0-blue.svg)](https://github.com/shivarajm8234/Fahhh)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

**Flash Course AI** is a premium browser extension designed to accelerate your learning experience. It combines intelligent module progression with absolute automation to help you complete courses in record time.

## 🚀 Key Features

- **⚡ 10x Velocity**: Automatically sets all course videos to 10.0x speed with smart unmuting.
- **📡 Smart URL Navigation**: Advanced algorithm that detects module numbering in URLs to jump ahead instantly.
- **🧪 Deep-Scan Engine**: Pierces Shadow DOMs and recursive structures to find "Next" buttons that standard extensions miss.
- **🛡️ Anti-Distraction Shield**: Automatically dismisses cookie banners, overlays, and modal popups.
- **🧠 Ollama Integration**: Connects to your local Ollama instance for intelligent quiz solving (Selective Mode).
- **🎨 Premium UI**: Dark-themed glassmorphism dashboard with real-time status logging.

## 🛠️ Installation

### 1. Build the Extension
Ensure you have Node.js installed, then run:
```bash
npm install
npm run build
```

### 2. Load into Brave/Chrome
1. Open Brave and go to `brave://extensions`.
2. Enable **Developer mode** (top right).
3. Click **Load unpacked**.
4. Select the `dist` folder generated by the build process.

## ⚙️ Requirements
- **Ollama**: Must be running at `http://localhost:11434`.
- **CORS**: Start Ollama with the following environment variable to allow extension access:
  ```bash
  OLLAMA_ORIGINS="chrome-extension://*" ollama serve
  ```

## 📝 Usage
1. Open your course page.
2. Click the **Flash Course AI** icon in your toolbar.
3. Select your preferred local model (e.g., `llama3`).
4. Click **Start Auto-Course** for automated navigation or **Solve Quiz** for testing help.

---
Built with ❤️ by [shivarajm8234](https://github.com/shivarajm8234)

## Expanding the ESLint configuration

If you are developing a production application, we recommend updating the configuration to enable type-aware lint rules:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
